import React from 'react';
import { Container, Typography } from '@mui/material';

const RoleDetail = () => {
  return (
    <Container>
      <Typography variant="h5">Role Details</Typography>
      <Typography>Placeholder for role details view.</Typography>
    </Container>
  );
};

export default RoleDetail;